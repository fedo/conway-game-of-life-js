/*
Game of Life API
- create(size: { width, height }, seed: [{ row, col }]) -> universe
- evolve(universe) -> universe
- view(universe) -> [{ row, col, alive: bool }]
*/

const create = ({size: {width, height }}, seed) => {

}
// => universe

const evolve = (universe) => {

}
// => universe

const view = (universe) => {

}
// => [{ row, col, alive: bool }]